@{
	var Index = int.Parse(Model.General.Index); 
	var Mod = Model.RootList[Index];
}

// Aaaa生成
@(Model.Out.Aaaa)


// Bbbb生成
@(Model.Out.Bbbb)

// Cccc生成
@(Model.Out.Cccc)

// Dddd生成
@(Model.Out.Dddd)