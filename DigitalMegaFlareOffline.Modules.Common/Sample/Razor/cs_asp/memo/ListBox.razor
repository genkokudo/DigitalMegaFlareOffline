@{
    var Index = int.Parse(Model.General.Index);
    var Mod = Model.RootList[Index];
}
// ListBox.razorの生成ソース
@* コメント：これは生成結果に改行も入らない *@
@* @foreach(){}の中での出力は、先頭に"@:"を書く。 *@
// @(Model.General.Index)